{
  "name": "cicd_demo",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.5",
    "@testing-library/react": "^13.4.0"
  },
  "scripts": {
    "start": "npm --prefix cicd_demo run start",
    "dev": "npm --prefix cicd_demo run dev",
    "build": "npm --prefix cicd_demo run build",
    "lint": "npm --prefix cicd_demo run lint",
    "preview": "npm --prefix cicd_demo run preview"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}

